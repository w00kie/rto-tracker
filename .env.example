# Convex Configuration
# Get this from running `bunx convex dev` or from your Convex dashboard
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud

# CI/CD and Deployment (GitHub Secrets & Cloudflare Pages)
# Generate from Convex Dashboard → Settings → Deploy Keys
CONVEX_DEPLOY_KEY=your-deploy-key-here

# Cloudflare Pages credentials
# Get from Cloudflare Account → API Tokens
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id

# Authentication (configured in Convex Dashboard)
# OAuth providers: Google and GitHub
# Set client IDs and secrets in Convex Dashboard → Settings → Authentication
# Redirect URLs:
#   Development: http://localhost:3000
#   Production: https://rto.w00kie.com
